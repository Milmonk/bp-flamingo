# ============================================================================
# BP-FLAMINGO: Python Dependencies (Python 3.11 / HPC Perun)
# ============================================================================
# NOTE: torch, torchvision and open-flamingo are installed separately
#       in setup_project.sh to avoid version conflicts.
# Install: pip install -r requirements.txt
# ============================================================================

# --- OpenFlamingo dependencies (flamingo itself installed from GitHub) ---
open-clip-torch>=2.20.0
transformers>=4.35.0
tokenizers>=0.15.0
einops>=0.7.0
huggingface-hub>=0.19.0

# --- Translation (MarianMT) ---
sentencepiece>=0.1.99

# --- Evaluation metrics ---
sacrebleu>=2.3.1          # BLEU, chrF
nltk>=3.8.1               # METEOR
bert-score>=0.3.13        # BERTScore
rouge-score>=0.1.2        # ROUGE (optional)

# --- Language detection ---
langdetect>=1.0.9

# --- Data handling ---
pandas>=2.1.0
numpy>=1.24.0
Pillow>=10.0.0
datasets>=2.14.0          # HuggingFace datasets

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Utilities ---
tqdm>=4.65.0
pyyaml>=6.0
jsonlines>=4.0.0

# --- Jupyter (for notebooks) ---
jupyter>=1.0.0
ipywidgets>=8.0.0